{
    "name": "Ollama",
    "image": "mcr.microsoft.com/vscode/devcontainers/base:0-ubuntu-20.04",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python"
            ]
        }
    },
    "postCreateCommand": "/bin/bash -c 'echo Running postCreateCommand... && curl -fsSL https://ollama.com/install.sh | sh && echo Finished running postCreateCommand. && sudo apt update && sudo apt install python3 python3-pip'",

    "hostRequirements": {
       "cpus": 2,
       "memory": "16gb",
       "storage": "32gb"
    }

}