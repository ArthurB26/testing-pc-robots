{
    "name": "Ollama",
    "image": "mcr.microsoft.com/vscode/devcontainers/base:0-ubuntu-20.04",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python"
            ]
        }
    },
    "postCreateCommand": "/bin/bash -c 'echo Running postCreateCommand... && curl -fsSL https://ollama.com/install.sh | sh && echo Finished running postCreateCommand. && sudo apt update && sudo apt install -y python3 python3-pip && ollama pull deepseek-r1:1.5b && pip install ollama && ollama pull llama3.2'",

    "hostRequirements": {
       "cpus": 2,
       "memory": "16gb",
       "storage": "32gb"
    }

}